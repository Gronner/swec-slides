[package]
name = "swec-polylux"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Felix 'Gronner' Bräunling <gronner@mailbox.org>"]
license = "MIT-0"
description = "A typst template for SoftWare Engineering Camp (SWEC) presentations"
repository = "https://github.com/Gronner/swec-polylux"
keywords = ["polylux", "template", "slides", "presentation"]
categories = ["presentation"]
compiler = "0.14.2"
exclude = ["*/.pdf"]

[template]
path = "template"
entrypoint = "slides.typ"
